{
  "name": "youtube-audio-arbiter",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for the unpacked YouTube Audio Arbiter extension.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check core.js && node --check service-worker.js && node --check content/controller.js && node --check popup/popup.js"
  }
}
